# packages/editor — architecture gap audit + execution plan

Findings dossier on SPI gaps, hardcoded type branches, feature leaks, and duplication in the owned-model editor, plus one epic that folds in every fix and an ordered sequence to run it. The legacy Lexical editor extraction is tracked separately. No code changed yet. Scope is `src/core` and `src/view`.

## The lens

The engine has one design rule (docs/016, /020, /021, /022): register, don't hardcode. A block registers a `NodeView` + `NodeDefinition` (objects) or a `StructuralNodeView` + `StructuralDefinition` (containers), and generic code holds no node-type knowledge. Two clean SPI surfaces already exist: `src/view/node-view.ts` (objects) and `src/view/structural-view.ts` (containers).

Two parts of the codebase already do this right, and they are the shape to copy. `caretInk` is a generic slot the selection overlay reads by walking ancestors with no per-type knowledge (`selection-overlay.tsx:171-181`). The insert menu enumerates `listInsertableNodes()` + `listInsertableStructuralNodes()`, so a registered node appears with zero edits (`editor-chrome.tsx:180-205`). Every gap below is a place that rule is broken or was left half-done.

## Part A — the table

The table spans 5 active files (~2,660 lines). The split is mostly correct layering; the only real defects are one misfiled file and the overlay leak.

| File | Lines | Layer | On an SPI? |
|---|---|---|---|
| `core/table.ts` | 276 | core: seed / import / export `StructuralDefinition`s | yes, via `tableStructuralDefinitions()` into `BUILT_IN_STRUCTURAL_DEFINITIONS` |
| `view/nodes/table.tsx` | 185 | view: live + resting render, `caretInk` | yes, via `registerStructuralView` |
| `view/table-operations.ts` | 1,324 | pure model command-builders (grid map, merge/unmerge, insert/delete) | no, imported directly |
| `view/table-controls.tsx` | 494 | live hover overlay (insert/delete/resize + table chrome) | no, hardcoded in react-view |
| `view/table-interactions.tsx` | 381 | cell-range selection + cell-action button | no, hardcoded in react-view |

The layering is fine where core stays table-agnostic: core gains no table verb, so table logic splits into a core definition and a view structural-view, both registered through the same path callout and list use (`nodes/index.ts:52-61`). Every block splits across core (definition) and view (node view) this way; callout does the same and nobody calls it scattered. So the table is not uniquely scattered.

The leak is `react-view.tsx:49-50, 341-342, 400-401`. It imports and mounts `<TableControls>` + `<TableInteractions>` in both the virtualized and non-virtualized branches. Table is the only block type whose name appears in the generic view orchestrator; every other block renders blind through the dispatcher. The cause is a missing SPI slot: the node SPI covers render, insert, caretInk, and chrome, but nothing for a view-level overlay (a portal with global pointer listeners). The two overlays had nowhere to register, so they sit in the JSX, duplicated across both branches.

## Part B — the toolbar API

The toolbar drives the model, not the DOM, and that part is solid. Toggles read `store.query({type:"is-mark-active"})` and dispatch `store.command({type:"toggle-mark"})` (`editor-chrome.tsx:328-354`), state stays live through a selection+commit subscription (`useToolbarVersion`, `:110-128`), and focus survives a press. The insert menu is fully SPI-driven (`:180-205`), so a registered node shows up without touching the toolbar.

The hardcoded half is what you sensed. Formatting marks (`FORMAT_BUTTONS`, `:37-48`), block types (`BLOCK_TYPES`, `:50-98`), and the list/indent buttons are static literal arrays. No `registerMark` or `registerBlockType` SPI exists, so adding a mark or a block type means editing this file. Insert is extensible; formatting and block-type are not.

There are also two chrome systems with no shared contract. The document toolbar lives in `editor-chrome.tsx`. The table builds its own floating chrome (layout select, structure menu, cell actions) by hand from `BlockChrome` / `ChromeButton` / `ChromeSelect` in `table-controls.tsx:391-459` and `table-interactions.tsx:271-378`, each with its own literals (`TABLE_LAYOUTS`, `FILL_COLORS`). They share idco-ui primitives but no contextual-toolbar abstraction, so each feature re-rolls its chrome.

## Part C — the findings (C1–C8)

C1. `list` / `listitem` / `quote` are half-migrated structural types. `structural-view.ts` says so outright: "quote/list/listitem keep hardcoded compat branches until migrated." Only `callout` fully crossed into the SPI; `BUILT_IN_STRUCTURAL_DEFINITIONS = [callout, ...table]` has no list and no quote. Their names stay hardcoded in 8+ generic spots: `core/compat.ts` (~286-389, 469, 509, 769-866), `view/resting-document.tsx:186-224`, `view/styles.ts:386`, `core/commands/shared.ts` (201, 230, 469-520), `core/commands/text.ts:218,253`, `core/store/editor-store.ts:1176`, `view/selection-overlay.tsx:647-651`. This is the worst rot risk: callout is the finished template, but the engine still has two ways to know a container exists.

C2. Marks have no SPI. The 11 marks (bold, italic, underline, strikethrough, code, highlight, sub, superscript, link, comment, glossary) are a closed union hardcoded in `core/model.ts:164`, `view/mark-render.tsx:51` (`MARK_NESTING_ORDER`), `view/mark-render.tsx` (`wrapMark` switch), `view/editor-chrome.tsx:37`, `view/context-menu.tsx:28`, and `core/bake.ts:144`. Same conceptual size as the node SPI, none of the infrastructure.

C3. Text-leaf block types are a closed set. `paragraph` / `heading` / `quote` / `listitem` (`TextLeafType`, `model.ts:191`) can't be extended; they are hardcoded in `editor-chrome.tsx:50`, `context-menu.tsx:41`, `core/markdown-shortcuts.ts:88-94`, `core/bake.ts:141`, and `selection-overlay.tsx:647`.

C4. Overlay SPI gap. The table-controls and table-interactions leak into react-view (Part A). The fix is a generic `renderOverlay` slot, modeled on `caretInk`.

C5. Toolbar and context-menu duplicate each other, and the copies have drifted. `FORMAT_BUTTONS` (`editor-chrome.tsx`) and `FORMAT_ITEMS` (`context-menu.tsx`) are the same 6-mark list, twice. `BLOCK_TYPES` (Paragraph, H1-H4, Quote) and `BLOCK_ITEMS` (Paragraph, H1, H2, Quote) already diverge: the context menu silently lacks H3 and H4. The list-toggle command logic is also copied in both.

C6. compat has two answers to "is this an object type." `isObjectNodeType(type, registry)` (`compat.ts:901`) is registry-driven, but `isBuiltInObjectCompatType(type)` (`:905`, used at `:869`) re-hardcodes code-block/media/post-ref/embed/table-of-contents. A registered custom object passes one check and fails the other, so round-trip can diverge for third-party nodes.

C7. `view/table-operations.ts` is misfiled: 1,324 lines, pure model, no React, imports only `../core`, yet it lives in the view layer. It belongs in core.

C8. `payload-import.ts` is a third dialect adapter with no SPI reuse. It hardcodes a type switch (upload, youtube, horizontalrule, list, table, code) and a registered custom node can't teach it to map itself.

(Dropped from the earlier draft: the "table needs a feature folder" complaint. The repo groups by role, not feature: `view/nodes/` holds every node view, `view/controllers/` holds hooks, `core/commands/` and `core/store/` are the only core subfolders. `nodes/table.tsx` already sits with `callout.tsx` and `list.tsx`. A `view/table/` feature folder would be the first such divergence, so the only real relocation is C7.)

## Decisions locked in this session

- Table home: the two framework-free files go to a new `core/table/` subfolder (consistent with `core/commands/`, `core/store/`): `core/table.ts` becomes `core/table/definitions.ts`, and `view/table-operations.ts` moves to `core/table/operations.ts`. The three React files stay in `view/` under the existing role-based layout (`nodes/table.tsx` stays; the two overlays register through the new `renderOverlay` slot). `core/` is framework-free and worker-safe (it runs `core/bake.worker.ts`), so the React files cannot move there.
- spike: keep `src/spike/` as is. It backs 2 stories (`engine-input`, `engine-flow`); no removal. — SUPERSEDED 2026-06-22: spike removed once its behavior bar was reabsorbed and its unique IME coverage folded into `engine-ime.spec.ts` (see F7).
- legacy: do not delete. Extract it to its own package, `packages/editor-legacy`, and shed Lexical from the owned package. Tracked separately from the SPI epic (see the Legacy extraction track).

## The epic — every SPI/cleanup fix, nothing deferred

One workstream list for the owned-model engine. Each item maps to a finding; all ship. Each carries a seam (shape + call sites + done-when) so intent survives context loss; the deep per-SPI design graduates to a `docs/0XX` doc when the workstream starts, per the project's SPI-first convention (lock the public shape before internals).

- W1. renderOverlay slot (C4). DONE. Added `renderOverlay?(args: { store; rootRef })` to both `StructuralNodeView` and `NodeView`, plus `listOverlayStructuralViews()`/`listOverlayNodeViews()` enumerators (mirroring `listInsertable*`); `react-view.tsx` enumerates them in `renderEngineOverlays(store, ref)` and mounts in both branches, replacing the hardcoded `<TableControls/>` + `<TableInteractions/>`. The table registers both overlays from one slot on the canonical `table` view only (the `editor-table` alias shares the one running overlay, so it mounts once). New arg types (`StructuralOverlayArgs`, `NodeOverlayArgs`) exported on the public surface; the enumerators stay orchestrator-internal (deep-imported). Verified: react-view imports neither table file, `pnpm check` green, 821/821 tests pass (3 new overlay-SPI tests in `engine-structural-spi.test.ts`). Reviewed by a read-only subagent (no blockers; added the missing slot test it flagged).
- W2. table model half → `core/table/` (C7). DONE. `git mv` of `core/table.ts` → `core/table/definitions.ts` and `view/table-operations.ts` → `core/table/operations.ts` (both R100, history preserved). Internal import fixups: `definitions.ts` (`./model`→`../model`, `./structural-registry`→`../structural-registry`); `operations.ts` (`../core` split into `../model` + `import type ... from "../store"`). Importers repointed: definitions in `structural-registry.ts:45` (the only one; `payload-import.ts`/`nodes/index.ts` were false positives — `nodes/index.ts "./table"` is the view); operations in `table-controls.tsx`, `table-interactions.tsx`, `text-block.tsx`, `tests/editor/engine-table.test.ts`. Chose deep imports (`../core/table/operations`) over a core-barrel re-export to avoid adding ~30 table symbols to `core/index.ts`; the editor already deep-imports core (the bake worker). The SPI guardrail test reads a fixed 6-file allow-list, so the moved files do not trip it. Verified: `pnpm check` green, 818/818 tests pass.
- W3. list/quote/listitem (C1). RESOLVED via Option C (decision 2026-06-22): reclassified as intrinsic core primitives, consolidated under a pattern, NOT migrated to the structural SPI and deliberately NOT opened to third-party block types. Rationale: the C1 audit overstated these as "rot." They cannot ride `StructuralDefinition` (always-structural, 1:1) because they are (a) primarily text leaves, (b) dual — a block-bearing quote/listitem becomes structural — and (c) one-to-many on import (a `<list>` flattens). Full SPI-ification would buy extensibility no host needs (the user confirmed they will not add custom flat-block types) at high cost + round-trip risk. But intrinsic must not mean wired-at-random, so the scattered compat branches are now consolidated: new `core/flat-blocks.ts` is the single patterned home — a declarative `FLAT_TEXT_LEAF_BLOCKS` table (each block's kept attr keys + dialect aliases) plus the flatten/dual dialect logic, reached through one `importFlatBlock(node, ctx)` entry that `compat.ts` calls with a `FlatBlockImportContext` (the same ctx-of-helpers pattern `StructuralDefinition` uses). The four near-identical text-leaf import arms collapsed into the table; `compat.ts` shed ~120 lines. Behaviour-preserving: `pnpm check` green, 831/831 tests pass.
- W3-intrinsic-by-design. The remaining type-name references are intrinsic editor/render SEMANTICS for these primitives, NOT rot, and stay (documented): list/quote Enter-Backspace-indent in `commands/text.ts`+`commands/shared.ts`, the list-layout republish in `editor-store.ts`, the flat-list run-numbering in `styles.ts`, and the flat/structural `<li>` render in `resting-document.tsx`. No editor makes per-type Enter/Backspace or list-numbering generically pluggable. The text-leaf aria role already moved to the W5 block-type registry. Reaching for a docs/0XX + contract redesign remains the only path if a real third-party flat-block requirement ever appears.
- W3-note. `listitem` is both a `TextLeafType` (flat list, docs/018 §2.10) and a structural type (nested); the migration keeps the flat-leaf render path and moves only the structural-container knowledge behind the SPI. Settle this shared identity with W5 in the docs/0XX before coding.
- W4. registerMark SPI (C2). DONE. New `view/mark-registry.ts` defines `MarkDefinition { kind; nestingRank; render; toolbar? }` + `registerMark`/`getMark`/`listMarks`/`markNestingRank`; `mark-render.tsx` now registers the 11 built-ins (`registerBuiltInMarks`, idempotent, self-called at module load for the standalone resting reader and also called by the view orchestrator so W6's toolbar can't race an empty registry) and `wrapMark` delegates to `getMark(kind).render(...)` with a neutral-span fallback for an unregistered kind. The 6 format marks carry `toolbar` meta (consumed by W6); link/comment/glossary/sub/super are render-only. `TextMarkKind` stays the persisted union in `model.ts:164` (compat needs the literals); render/toolbar derive from the registry. SCOPING CORRECTION: the bake comment/glossary branch (`bake.ts`) is NOT moved — it classifies annotation marks for the search index, a core concern that cannot import a React view registry (bake runs in `bake.worker`); de-hardcoding it would need a separate core-side mark descriptor, out of this view-layer SPI. Registry + arg types exported on the public surface. Verified: `pnpm check` green, 825/825 tests pass (registry contract + custom-mark + fallback tests). Reviewed by a read-only subagent; applied its fixes (explicit `registerBuiltInMarks` entry point, dropped a dead `LinkMode` re-export, added the override + fallback tests).
- W5. registerBlockType SPI (C3). DONE. New `view/block-type-registry.ts` defines `BlockTypeDefinition { id; blockType; tag?; label; icon; preview?; chooser?; ariaRole }` + `registerBlockType`/`getBlockType`/`listBlockTypes`/`blockTypeKey`/`blockTypeRole` + `registerBuiltInBlockTypes` (idempotent, self-called at module load and called by the orchestrator, mirroring W4). Built-ins: paragraph, heading h1-h4, quote (all `chooser`), and the list item (registered for `blockTypeRole`, not a chooser entry — lists toggle via their own buttons). `selection-overlay.ts` aria role now derives from `blockTypeRole` (behaviour-identical). `id` = `${blockType}:${tag}` matches the toolbar's existing key, so W6 can swap directly. SCOPING (same boundary as W4): this is the VIEW chrome+aria registry; the CORE block concerns stay in core — markdown autoformat prefixes (`markdown-shortcuts.ts`, which carry list-specific data like a bullet item's two prefixes) and the heading TOC level (`bake.ts`, worker-safe) cannot import a view registry and were not moved, so there is no `markdownPrefix` field. Thus "adding a block type is one registration" holds for the chooser + aria; a markdown-autoformattable block still needs a `markdown-shortcuts` entry. Toolbar/context-menu dedup is W6. Verified: `pnpm check` green, 829/829 tests pass. Reviewed by a read-only subagent; applied its fixes (test registers the custom type without `chooser` to avoid leaking a phantom item into W6's toolbar set; clarified `blockTypeRole` is per-`blockType`).
- W6. dedup toolbar + context-menu (C5). DONE. `editor-chrome.tsx` and `context-menu.tsx` deleted their `FORMAT_BUTTONS`/`FORMAT_ITEMS` and `BLOCK_TYPES`/`BLOCK_ITEMS` literal arrays and now read `listMarks().filter((m) => m.toolbar)` + `listBlockTypes().filter((b) => b.chooser)`. The H3/H4 drift is fixed at the source — the context menu now shows every chooser block type, since both surfaces read the one registry. The duplicated list-toggle command is also closed: a shared `listToggleCommand` in `view/chrome-commands.ts` is used by the toolbar's bullet + numbered buttons and the context menu's List item. Verified: `pnpm check` green. Reviewed by a read-only subagent (no blockers; it flagged the list-toggle dup as the one open part of C5, now closed).
- W7. compat object single-source (C6). DONE. Deleted `isBuiltInObjectCompatType`; `isBlockChild`/`hasBlockChildren` now take the `BlockRegistry` and use `isObjectNodeType(type, registry)`, threaded from `state.registry` (and via a closure for the structural compat context). One registry-driven check now decides "is this an object block child," so a registered custom object round-trips like a built-in — and it also fixes a latent bug where the hardcoded list omitted `divider`, so a `divider` nested in a quote/list was misread as inline. Verified: `pnpm check` green, 830/830 tests pass (new test: a custom object nested in a quote imports as a block child).
- W8. payload per-node hook (C8). DONE. Added optional `fromPayload?(node): RichTextCompatNode | null` to `NodeDefinition` and `StructuralDefinition`; `payload-import.ts`'s `mapBlock` default case now calls `mapViaRegistry(node)` (tries each globally-registered definition's `fromPayload`) before dropping, so a host's custom node maps its own Payload dialect type with no edit to the importer. Built-in dialect types keep their explicit `mapBlock` mappings; iteration is object defs then structural, first non-null wins. Verified: `pnpm check` green, 831/831 tests pass (new test: a registered custom node's Payload type maps via its hook instead of being dropped). Reviewed by a read-only subagent; applied its fixes (added `unregisterGlobalNodeDefinition` + test cleanup to stop the global-registry leak; documented the iteration order and that a structural hook owns converting its own dialect subtree).

## Execution sequence — run in this order

Do the relocation before the semantic edits so later diffs land in final locations, then the small proven slice, then the big migration, then the new SPIs and their consumers, then the adapter hook. Run `pnpm check` at the end of every phase (format, lint, dup gate, typecheck, test, build).

1. Phase 0, relocation (W2). DONE (2026-06-21). Created `core/table/`, moved definitions + operations, fixed imports, `pnpm check` green, 818/818 tests pass. No behavior change; pre-positions every later table edit.
2. Phase 1, the overlay slice (W1). DONE (2026-06-21). Added `renderOverlay`, registered the two table overlays through it, stripped them out of react-view. `pnpm check` green, 821/821 tests pass.
3. Phase 2, finish the structural migration (W3). DEFERRED (see W3 above) — contract redesign + design doc needed; the cleanable text-leaf parts move under W5 instead. The remaining epic ships as 5 commits: W1 (done) + W4 + W5 + (W6+W7) + W8.
4. Phase 3, the missing SPIs (W4, W5). registerMark, then registerBlockType. New capability, no consumer rewrite yet.
5. Phase 4, dedup the consumers (W6, W7). DONE (2026-06-21). Toolbar + context-menu read the W4/W5 registries; compat object check collapsed to the registry. H3/H4 drift fixed. `pnpm check` green, 830/830 tests.
6. Phase 5, the adapter hook (W8). DONE (2026-06-22). Added the payload per-node `fromPayload` hook + registry dispatch. `pnpm check` green, 831/831 tests.

Status: epic complete. W1, W4, W5, W6, W7, W8 done (5 commits); W3 resolved via Option C (intrinsic-primitives reclassification + `core/flat-blocks.ts` consolidation, see W3). Remaining future work: only the gated legacy-extraction track below (and a docs/0XX contract redesign IF a real third-party flat-block need ever appears).

## Folder-contract audit (2026-06-22) — the file/folder layout, not the API

The SPI epic above fixed the internal *API* (register-don't-hardcode). The *file and folder contract* is still messy and is tracked here. Scope check confirmed the layer boundaries hold: no `core/` file imports `view/`/`legacy/`/`spike/`; `legacy/` imports neither `core/` nor `view/` (a clean cut); `view/` only mentions legacy in comments. So `core/` (framework-free, grouped by concern in `commands/`/`store/`/`table/`/`vendor/`) is the layout the rest of the tree should imitate. The findings below are where it doesn't. Nothing here is started. F6 is the existing Legacy extraction track below — not duplicated.

- F1. `view/` is a 34-file flat dump (~8,791 lines). `view/` has only two subfolders (`nodes/`, `controllers/`); everything else is flat — the 4 SPI registries, the editor shell (`react-view`, `owned-model-editor`), chrome (`editor-chrome`, `context-menu`, `chrome-commands`, `callout-chrome`, `link-popover`, `find-bar`), overlays (`selection-overlay`, `geometry`, `gap-cursor`, `touch-selection`), table overlays, object config, and render primitives all in one directory. `core/` earned `commands/`/`store/`/`table/`; `view/` never got the same treatment. This is the primary "messy" feeling. Fix: subdivide `view/` by role to mirror `core/` — natural clusters are `view/spi/` (the 4 registries), `view/chrome/`, `view/overlays/`. `nodes/`+`controllers/` already prove the pattern.
- F2. The curated-barrel contract is contradicted by `package.json`. `src/index.ts:9-12` claims deep internals "remain importable from the package's `core/` and `view/` modules for advanced use," but `package.json` `exports` only exposes `"."` and `"./legacy"` — no `./core` or `./view` subpath. The sentence is true for in-package code, false for consumers. Fix: pick one — either add the subpaths (or a `./internal`) to the `exports` map, or rewrite the `index.ts` header to say internals are package-private.
- F3. `sideEffects: false` is violated by module-load registration. `package.json:5` declares `"sideEffects": false`, but `mark-render.tsx:138` and `block-type-registry.ts:161` call `registerBuiltInMarks()`/`registerBuiltInBlockTypes()` at top level. A bundler may tree-shake those modules and drop the registration, so the standalone `RestingDocument` reader (which relies on the load-time self-call per W4) could render against an empty registry; the full editor is safe only because the orchestrator re-calls them (`react-view.tsx:81,84`). Fix: either list those registry modules in a `"sideEffects": [...]` array, or have the resting reader call `registerBuiltInMarks()` explicitly instead of relying on the import side effect.
- F4. The EditContext polyfill is misfiled under `core/`. `core/vendor/editcontext-polyfill/` is a browser/DOM polyfill, but `core/` never uses it — its only consumers are `view/text-block.tsx` and `spike/`. So `view/` reaches into `core/vendor/`, a layer jump into the framework-free tree for something framework-adjacent; it sits under `core/` only by accident of where it was vendored. Fix: move it to a top-level `src/vendor/` (or under `view/`) so `core/` owns nothing it doesn't use. Gated by F7 (spike still pins the current location).
- F5. Per-block chrome/overlays leak out of `nodes/`, contradicting Part A's "table isn't special" claim. Table's view pieces span three places — `core/table/`, `view/nodes/table.tsx`, and two flat files `view/table-controls.tsx` + `view/table-interactions.tsx`. Same with callout: `nodes/callout.tsx` + flat `callout-chrome.tsx`. So the role-folder rule is followed for a block's *render* but not for its *chrome/overlay*. This revises the Part A / decision-2026-06-21 stance that resisted a `view/table/` folder: the data shows block-specific UI already escapes the role layout, so co-locating it (a per-block folder, or at least pulling the chrome into `nodes/`) is more honest than three scattered files. Worth a deliberate decision, not the current default.
- F7. `spike/` pins F4 and is still load-bearing. RESOLVED 2026-06-22 — `src/spike/` is removed. Its deletion criterion was met: the input/IME/caret behavior was reabsorbed into the owned engine (`selection-overlay`/`styles` carry "Reabsorbed from the Phase 2 spike" comments) and the owned engine has its own e2e behavior bar (`engine-ime`, `engine-caret`, `engine-mobile`, `engine-editing`, …). Removed: `packages/editor/src/spike/` (6 files), the 2 spike Ladle stories (`engine-input`, `engine-flow`), and their 2 e2e specs. Before deleting, the genuinely-unique coverage — the 5 real-IME polyfill regression replays (Firefox Telex dup, Telex selection-desync, iPad Vietnamese + word-delete, Android Gboard delete-selected-range, Android Chrome redundant-delete-twin) — was FOLDED into the owned `tests/e2e/engine-ime.spec.ts`, retargeted from the spike host onto the shipped owned-model forced-polyfill block (same vendored polyfill). Assertions deliberately target only the FINAL converged model text (`blockTexts[id]`), the stable contract under the owned engine's diff-based input reconcile. VERIFIED GREEN 2026-06-22 on chromium + webkit + firefox (the spike's own matrix): all 5 folded tests pass against the shipped owned engine, including the selection-desync (`"xin o"`) and Gboard redundant-twin cases that the diff-based reconcile handles. One port fix was needed: the leaf must be cleared through the polyfill `deleteContentBackward` input path, not a hardware Backspace, because a touch profile (the iPad test) does not bind keydown deletion. The third referrer, `editor-backspace.perf.spec.ts`, never imported spike (it targets the owned `rich-text-editor` full-editor story; the earlier note that it imported spike was wrong). F4 was already done in CP6, so spike no longer pins anything.

Suggested order: F2+F3 first (cheap, correctness-adjacent contract fixes), then F1 (the `view/` subdivision), then F5 (co-locate block chrome — needs the deliberate decision), then F4 (after F7 frees the polyfill). F6 (Legacy extraction) is independent and highest-value; it ends with a cross-repo release. F8 + the `core/` reorg is its own track below.

## Core-layout audit + plan (2026-06-22) — F8 and the core/ reorg

`core/` is 19 flat top-level files (~5,100 lines) plus four subfolders (`commands/`, `store/`, `table/`, `vendor/`). The subfolders prove the by-concern grouping works, but most of `core/` never got it, so unrelated concerns sit flat next to each other and the SPI core-halves are buried (F8). The good news from the research: `core/` is genuinely framework-free (zero React imports — worker-safety holds) and its internal dependency graph is a clean acyclic layering, so this is a behavior-preserving relocation, not a redesign. Verified dependency layers (each depends only on lower ones): L0 `model.ts` (the spine), `scheduler.ts` (standalone — imports nothing), `url-safety.ts`, `virtual-range.ts`; L1 (→model) `steps`, `marks`, `markdown-shortcuts`, `flat-blocks`, `registry`, `structural-registry`; L2 `mapping` (→model,steps), `bake` (→model,registry); L3 `store/` + `selection` + `commands/`; L4 `compat`, `payload-import`; L5 `editor-handle`, `bake-service`, `bake.worker`.

F8 (the registry split) is the trigger. `core/structural-registry.ts` is purely the structural SPI, but `core/registry.ts` (581 lines) does two unrelated jobs under one generic name: (1) the object block SPI — `NodeDefinition` (`:56`), `BUILT_IN_OBJECT_DEFINITIONS` (`:196`), `registerGlobalNodeDefinition`/`unregisterGlobalNodeDefinition`/`globalNodeDefinitions` (`:165-175`); and (2) the generic runtime registry — the `BlockRegistry` class (`:105`) + `createDefaultBlockRegistry` (`:180`) that the store/bake/compat/editor-handle all depend on. Consumer split proves the two audiences: `payload-import` pulls `globalNodeDefinitions` (the SPI), while `editor-handle`/`bake`/`bake-service`/`compat`/`store` pull `BlockRegistry`/`createDefaultBlockRegistry` (the runtime). The generic name exists because of job 2, and that is what buries the object SPI inside it. Rider: `BlockDefinition = NodeDefinition` (`:102`) is a likely-dead alias — confirm or drop.

Target `core/` layout (by concern, mirroring `commands/`/`store/`/`table/`):

```
core/
  model/        model.ts, steps.ts, mapping.ts, marks.ts          (the document spine + transaction primitives + position math)
  registry/     object-registry.ts (NEW — object SPI),            (the node-type SPI core-halves, symmetric)
                block-registry.ts (NEW — BlockRegistry + createDefaultBlockRegistry, the shared runtime),
                structural-registry.ts (moved), flat-blocks.ts (moved — intrinsic flat-block table, W3)
  commands/     (exists)
  store/        (exists) + selection.ts (moved in — it depends on store)
  compat/       compat.ts, payload-import.ts                       (import/export dialect adapters)
  bake/         bake.ts, bake-service.ts, bake.worker.ts          (search-index/TOC bake + worker entry)
  table/        (exists)
  scheduler.ts  (stays at root — standalone runtime, the largest core file at 815 lines)
  url-safety.ts, virtual-range.ts  (stay at root — tiny cross-layer leaf utils; a core/util/ is optional, two files is thin)
  index.ts      (barrel — PUBLIC SURFACE UNCHANGED; only its internal re-export paths move)
  vendor/       editcontext-polyfill — F4 wants this OUT of core/ entirely (→ src/vendor/), since core never uses it
```

Phases (behavior-preserving; `git mv` to preserve history per the W2 precedent; `pnpm check` green at the end of each). STATUS: ALL DONE 2026-06-22 — CP1-CP6 landed in one contiguous run, full `pnpm check` green (format, lint, dup 0.9%/3%, typecheck, 831/831 tests, all builds incl. editor), every move a git-detected rename so history is preserved.

- CP1. F8 — registry split + `core/registry/` folder. DONE. Split `registry.ts` into `object-registry.ts` (object SPI) + `block-registry.ts` (`BlockRegistry` + `createDefaultBlockRegistry`); created `core/registry/` with an `index.ts` barrel (`export *` of object + block + structural), moved `structural-registry.ts` + `flat-blocks.ts` in. `object-registry.ts` ↔ `structural-registry.ts` are now mirror files. `compatObjectFromValue` is exported from `object-registry` so `block-registry`'s `toCompatObject` fallback shares the one shape. The `BlockDefinition` alias was NOT dead (public export at root `index.ts`, used in `engine-model.test.ts`) so it was kept. Importers of `./registry` were untouched (folder resolves); only `structural-registry`/`flat-blocks` deep-importers moved to the barrel/`registry/flat-blocks`.
- CP2. `core/model/`. DONE. `git mv` model/steps/mapping/marks into `core/model/` + an `index.ts` barrel. The cluster is internally closed (the four import only each other), so their mutual `./model`/`./steps` imports were unchanged; only external `../steps`/`../mapping` importers in `store/` + `commands/shared.ts` and the `core/index.ts` re-exports moved to the `./model` barrel. `commands/index.ts`'s `./marks` is `commands/marks.ts` (a different file) and was correctly left alone. Test dynamic-import `core/steps` repointed to `core/model/steps`.
- CP3. `core/compat/`. DONE. `git mv` `compat.ts` + `payload-import.ts` into `core/compat/` + barrel. Fixed the deep `../registry/flat-blocks` import (the bulk `./registry`→`../registry` rule did not catch the `/flat-blocks` suffix — watch for this on suffixed specifiers). The SPI guardrail test in `engine-table.test.ts` reads `compat.ts` by path; its allow-list entry was updated to `compat/compat.ts`.
- CP4. `core/bake/`. DONE. `git mv` `bake.ts` + `bake-service.ts` + `bake.worker.ts` into `core/bake/` + barrel (the worker entry is NOT in the barrel). The runtime worker URL was updated as flagged: `react-view.tsx:461` now does `new Worker(new URL("../core/bake/bake.worker.ts", import.meta.url))` (plus the `:113` comment). NOTE: unit tests use the loopback bake service, so the real `new Worker(URL)` path is only exercised by the bundler/app — `pnpm build` (tsc) passed, but the live worker spawn should be confirmed once in the running app (residual verification).
- CP5. `core/store/` tidy. DONE. `git mv` `selection.ts` into `core/store/`; it imports `ROOT_NODE_ID`/`EditorStore` directly from `./editor-store` (not the store barrel, to avoid a within-folder self-cycle) and `../model`. `store/index.ts` re-exports `collectSelectionText`/`orderedTextLeaves`; `core/index.ts` reads them from `./store`.
- CP6. F4 — move `vendor/editcontext-polyfill/` out of `core/` to `src/vendor/`. DONE (the polyfill is self-contained + React-free, so moving it now did not need F7 to clear first; spike's import was repointed alongside). Updated `view/text-block.tsx`, `spike/editcontext-host.ts`, the `editcontext-polyfill.test.ts` import, and `VENDOR.md`. GOTCHA HIT: the polyfill had an oxlint override (`no-underscore-dangle`, `typescript/no-unsafe-declaration-merging` off) scoped to the old `core/vendor/...` path in `.oxlintrc.json:31`; moving it broke that scope and lint failed until the override path was updated to `packages/editor/src/vendor/editcontext-polyfill/**`. F7 (spike still imports the polyfill, now from `src/vendor/`) remains open as before.

Guardrails / risks:

- The `core/index.ts` barrel keeps the public surface byte-identical (it re-exports); only its own internal import paths change, so the root `index.ts`, `package.json` exports, and external consumers are untouched. This is what makes the reorg safe to ship without a release.
- W2 locked deep imports over a fat barrel, so files are deep-imported across the package. Cross-layer deep imports to repoint: `view/react-view.tsx` (→ `core/scheduler`, `core/bake.worker`), `view/controllers/use-virtual-window.ts` (→ `core/virtual-range`, `core/scheduler`), `view/text-block.tsx` (→ `core/vendor/...`). `url-safety` is deep-imported from both core and view.
- Worker-safety is the invariant: `core/` must stay React-free (verified zero React imports today). The reorg only moves files, but re-confirm `compat/` and `bake/` gain no React import — `bake.worker` runs in a Worker.
- W2 mentioned an SPI guardrail that reads "a fixed 6-file allow-list"; the 2026-06-22 grep did not locate it (not in the `engine-*-spi` tests, not in `scripts/`). Before CP1/CP2 move SPI files, find that allow-list (or confirm it was the dup-gate/architecture-lint config) and update it, or the moved registry files trip it.

Track order vs the rest: CP1 (F8) can land anytime — it is independent of the view-side F1-F5. The full `core/` reorg (CP2-CP6) is best done as one contiguous run so deep-import churn lands once. F4/CP6 stays gated on F7.

## View-layout audit + plan (2026-06-22) — the F1/F5 reorg, deepened

This expands the F1 (flat `view/`) and F5 (per-block chrome leaks) stubs into a concrete plan, the same shape as the core track above. `view/` is 34 flat top-level files (~8,791 lines) plus `nodes/` (10) and `controllers/` (10). The dependency scan shows the same good news as core: the internal graph is acyclic and the clusters are already real — they just aren't folders. Verified layering (each tier imports only lower tiers): T0 leaves (`geometry`, `styles`, `raf`, `types`, `store-hooks`, `gap-cursor`, `paste-html`, `chrome-commands`, `object-data`, `upload-context`, `use-autosave` — each → core/idco-ui only); T1 the SPI registries (`structural-view`, `mark-registry`, `block-type-registry` → core; `node-view` → `structural-view`); T2 overlays (`selection-overlay`, `touch-selection`, `navigation` → spi + leaves; never import render); T3 render (`mark-render`, `text-block`, `object-block`, `resting-document`, `object-config`, `block-dispatch` → spi + overlays + leaves); T4 shell (`react-view` → render + spi + overlays + `nodes/` + `controllers/`; `owned-model-editor` → chrome + `react-view`). `chrome` (`editor-chrome`, `context-menu`, `link-popover`, `find-bar`) sits beside render, depending only on spi + leaves. `nodes/` and `controllers/` are imported only by `react-view` (low blast radius).

Findings:

- VF1 (= F1, confirmed). `view/` never got the by-role folders `nodes/`/`controllers/` proved out. The clusters above are latent. Highest-value grouping is the SPI: `node-view`, `structural-view`, `mark-registry`, `block-type-registry` are THE host extension surface and sit buried among 30 unrelated files.
- VF2 (= F5, deepened — the real smell). Per-block chrome/overlays are flat in `view/` while the block's node view (in `nodes/`) reaches UP to import them: `nodes/table.tsx` → `../table-controls` + `../table-interactions`; `nodes/callout.tsx` → `../callout-chrome`. This upward `nodes/ → flat view/` edge is the only place the role layout inverts. Co-location fixes it: a `nodes/table/` folder (`table.tsx` + `table-controls.tsx` + `table-interactions.tsx`) and `callout-chrome.tsx` beside `callout.tsx` (or `nodes/callout/`).
- VF3 (NEW — a per-type leak in the most generic render file). `text-block.tsx` (693 lines, the generic text renderer) imports `tabWithinTable` from `core/table/operations` and branches on table context for Tab (`:50`, `:577-579`). This is the keyboard analogue of the overlay leak W1 fixed via `renderOverlay`: there is no SPI slot for block-specific key handling, so table's Tab logic is welded into generic code. Options: add a key-handling slot to the structural SPI, or document it as intrinsic editor semantics (like the W3-intrinsic-by-design call for list/quote Enter/Backspace). Lower priority than VF1/VF2 and is a design decision, not a pure relocation.
- VF4 (NEW — `navigation.ts` size + home). At 682 lines it is the 2nd-largest view file; it is caret/grapheme/word/line navigation over the DOM, depends only on `geometry`, and is on the public surface (`applyEditContextText`, `lineRangeAt`, `nextGraphemeBoundary`, …). It pairs naturally with `geometry`/`gap-cursor` in the overlays/interaction cluster; flag it for a look (possible internal split) when that folder forms.
- VF5 (interacts with F3). `mark-render.tsx` and `block-type-registry.ts` self-call `registerBuiltIn*` at module load (the F3 side-effect). The reorg must preserve that load-time registration when these files move into `render/` and `spi/` — `react-view` also calls them explicitly, so the editor is safe, but the standalone resting reader relies on the import side effect. Resolve F3 (mark the modules in `sideEffects`, or register explicitly) as part of, or before, this reorg.

Target `view/` layout (by role + dependency tier, mirroring `nodes/`/`controllers/`):

```
view/
  spi/         node-view.ts, structural-view.ts, mark-registry.ts, block-type-registry.ts   (the host extension surface)
  render/      mark-render.tsx, text-block.tsx, object-block.tsx, block-dispatch.tsx, resting-document.tsx, object-config.tsx
  overlays/    selection-overlay.tsx, touch-selection.tsx, navigation.ts, geometry.ts, gap-cursor.ts
  chrome/      editor-chrome.tsx, context-menu.tsx, link-popover.tsx, find-bar.tsx, chrome-commands.ts
  nodes/       (exists) + co-locate per-block chrome (VF2): nodes/table/{table,table-controls,table-interactions}.tsx; callout-chrome.tsx beside callout.tsx
  controllers/ (exists)
  owned-model-editor.tsx, react-view.tsx, index.ts   (the component, the orchestrator, the barrel — stay at root)
  shared leaves stay at root (or an optional view/shared/): styles.ts, types.ts, store-hooks.ts, raf.ts, paste-html.ts, upload-context.tsx, use-autosave.ts, object-data.ts
```

Phases (behavior-preserving; `git mv` + a per-folder `index.ts` barrel + repoint deep imports + `pnpm check` each, exactly as CP1-CP6 ran). STATUS: ALL DONE 2026-06-22 — VP1-VP6 landed in one contiguous run, full `pnpm check` green (format, lint, dup 0.9%/3%, typecheck, 833/833 tests, all builds incl. editor), every move a git-detected rename so history is preserved. Final `view/` root holds only the shell (`owned-model-editor`, `react-view`, `index.ts`) + cross-cutting leaves (`styles`, `types`, `store-hooks`, `raf`, `paste-html`, `upload-context`, `use-autosave`, `object-data`); everything else is in a purpose-named folder.

- VP1. `view/spi/` — DONE. Moved the 4 registries + an `index.ts` barrel (`export *`). `node-view`→`structural-view` internal edge stayed sibling; the 3 SPI tests + every view/nodes importer repointed to the barrel. The extension surface is now one folder.
- VP2. `view/chrome/` — DONE. `editor-chrome`, `context-menu`, `link-popover`, `find-bar`, `chrome-commands` + barrel. Only `owned-model-editor` + `index.ts` imported these, so external churn was tiny; `chrome-commands` stayed sibling to its two callers.
- VP3. `view/overlays/` — DONE. `selection-overlay`, `touch-selection`, `navigation`, `geometry`, `gap-cursor` + barrel. Repointed `text-block`, `react-view`, `index.ts`, all of `controllers/`, and 4 tests. `gap-cursor.ts` confirmed import-free (pure helpers). VF4 (`navigation.ts` size) left as a future internal-split note, not acted on.
- VP4. `view/render/` — DONE. `block-dispatch`, `text-block`, `object-block`, `object-config`, `resting-document`, `mark-render` + barrel. GOTCHA: `text-block` imports the vendor polyfill via `../vendor`, which the cross-folder `../core`→`../../core` rule did not catch — fixed by hand to `../../vendor/editcontext-polyfill` (same suffix-specifier trap as CP3's `flat-blocks`). The F3 load-time mark registration is preserved through the barrel (documented in `render/index.ts`); the proper F3 sideEffects fix is still its own follow-up.
- VP5. `nodes/` co-location (VF2) — DONE. Created `nodes/table/` (`table.tsx` + `table-controls.tsx` + `table-interactions.tsx` + barrel) and `nodes/callout/` (`callout.tsx` + `callout-chrome.tsx` + barrel); `nodes/index.ts`'s `./table`/`./callout` imports resolve to the new barrels unchanged. This is the deliberate reversal of the Part A "no `view/table/`" stance — done INSIDE `nodes/` (a per-block feature folder), not as a top-level feature folder. The upward `nodes/ → flat view/` edge is gone; each block with chrome is now a self-contained unit.
- VP6. VF3 — DONE via the SPI route (not documented-intrinsic). Added `handleTab?(args: StructuralTabArgs): boolean` to `StructuralNodeView` + a `listTabHandlers()` enumerator (mirroring W1's `renderOverlay`/`listOverlayStructuralViews`). The table's Tab-cell-nav moved into `nodes/table/table.tsx` (registered on the canonical `table` view only, like its overlay); `text-block` now enumerates handlers and dispatches generically, with zero table knowledge (no more `core/table/operations` import). Behaviour identical because `tabWithinTable` already self-checks. `StructuralTabArgs` exported on the public surface (mirrors `StructuralOverlayArgs`); 2 contract tests added (831→833).

Guardrails (same as the core reorg): the `view/index.ts` barrel kept the public surface unchanged, so `package.json` exports and consumers are untouched (no release). The architecture lint scopes `core/` only, so view files had no React-ban concern. One test path-coupling existed (the SPI deep-imports in `engine-*-spi` + `engine-mark-render`/geometry/navigation), all repointed.

## Legacy extraction track — separate, gated, large

Goal: move `src/legacy/` (59 files / 11,442 lines, the Lexical editor) out of `@quanghuy1242/idco-editor` into a new package `packages/editor-legacy`, so the owned engine stops carrying Lexical. This is independent of the SPI epic and does not block it. Order it whenever, but it ends with a cross-repo release because it changes a published export.

STATUS: extraction DONE 2026-06-22 (steps 1-5). Owned `packages/editor/src` has ZERO Lexical imports and `packages/editor/package.json` has ZERO Lexical deps (verified: the only remaining `lexical` strings in editor/src are a `"lexical-table"` Payload dialect literal + two reference-impl comments in `core/table/operations.ts`). New package `@quanghuy1242/idco-editor-legacy` owns all 10 Lexical deps and builds to its own dist. Full `pnpm check` green: format, lint, dup 0.9%/3%, typecheck, 833/833 tests, 5 package builds incl. `editor-legacy`. All 59 legacy files moved as git-detected renames (history preserved). ONLY step 6 (the cross-repo release/publish) remains — deliberately NOT done here (outward-facing).

Why it pays off: the whole Lexical dependency block in `packages/editor/package.json` (`@lexical/link`, `@lexical/list`, `@lexical/mark`, `@lexical/markdown`, `@lexical/react`, `@lexical/rich-text`, `@lexical/selection`, `@lexical/table`, `@lexical/utils`, `lexical`, all `0.45.0`) existed only for legacy. After extraction the owned package shed all of them.

Steps:

1. Verify the cut line. DONE — confirmed `legacy/` imports nothing from `core`/`view`/`spike`/`vendor` and vice versa, and all Lexical imports were legacy-only. The move was mechanical.
2. Scaffold `packages/editor-legacy`. DONE — `package.json` (`@quanghuy1242/idco-editor-legacy`, v0.1.19) with the 10 Lexical deps + `idco-ui`/`idco-lib` (workspace) + `react-aria-components`, `react`/`react-dom` peers; `tsconfig.json` + `tsconfig.build.json` mirroring the editor package (paths → `lib`/`ui` dist d.ts). Wired into root `tsconfig` paths + `vitest.config` aliases (the `-legacy` hyphen avoids any prefix clash with the `idco-editor` alias). `pnpm install` registered the 6th workspace project.
3. Move the tree. DONE — `git mv src/legacy → editor-legacy/src` and `src/legacy.ts → editor-legacy/src/index.ts`; fixed the entry's `./legacy/` → `./` self-paths and rewrote its header. All other legacy imports were relative, so they survived the move untouched.
4. Repoint tests. DONE — the 18 deep-importers (`packages/editor/src/legacy/...` → `packages/editor-legacy/src/...`) PLUS 6 root-barrel importers that pulled legacy NAMES from `@idco/editor` (`comment`, `editor-foundation`, `rich-text-editor`, `selection-actions`, `engine-ids` → `@quanghuy1242/idco-editor-legacy`; and `engine-parity`'s AC5 test rewritten to assert the new contract — owned engine on the root, `RichTextEditor` only in the legacy package). No stories imported legacy.
5. Remove legacy from the owned package. DONE — deleted the root re-export block in `packages/editor/src/index.ts` (replaced with a note), updated the barrel header, removed the `./legacy` subpath export + all 10 Lexical deps from `packages/editor/package.json`. CONSUMER CONTRACT CHOSEN: full removal (no thin `@quanghuy1242/idco-editor/legacy` re-export) — consumers import `@quanghuy1242/idco-editor-legacy` directly, so the owned package has no Lexical edge at all. Also updated the architecture lint (`scripts/oxlint-js-plugins/architecture.js`): added `isEditorLegacyPackageSource`, folded it into the `@idco` boundary rule, and repointed the Lexical `registerUpdateListener` rule (+ the `editor-performance.ts` exemption) from `editor/src/legacy` to `editor-legacy/src` since the Lexical code moved.
6. Cross-repo release per AGENTS.md: `pnpm dev:link` in the consumer, prove `pnpm check`, bump every publishable `package.json` (now including `editor-legacy`) to the same version, tag, push, then `pnpm dev:unlink`. NOT DONE — this is the outward-facing publish step; the consumer (content-api/auth) must switch any `@quanghuy1242/idco-editor/legacy` or deprecated-root-name imports to `@quanghuy1242/idco-editor-legacy` when it repins. This is the breaking part of the change and is left for a deliberate release.
