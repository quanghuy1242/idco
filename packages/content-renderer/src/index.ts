/**
 * TODO: implement the lightweight public content renderer planned by
 * content-api Track X1.
 *
 * This package intentionally exists now so consumers and docs can point at the
 * permanent `@idco/content-renderer` home instead of creating another ad hoc
 * renderer package later.
 */
export type ContentRendererPlaceholder = {
  readonly plannedPackage: "@idco/content-renderer";
};
