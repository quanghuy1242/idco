/**
 * TODO: implement shared editor primitives planned by content-api Track X.
 *
 * The existing `@idco/ui` package carries today's extracted CodeEditor and
 * admin primitives. This package is reserved for editor-specific APIs once
 * Lexical/content node editing becomes its own stable surface.
 */
export type EditorPlaceholder = {
  readonly plannedPackage: "@idco/editor";
};
