/**
 * The callout block — its full view feature (note.md VP5).
 *
 * One folder, one block: the callout's structural node view plus its floating
 * chrome, co-located (VF2). `nodes/index.ts` imports the structural view here.
 *
 * - `callout.tsx`        — the structural node view (live + resting render)
 * - `callout-chrome.tsx` — the callout's floating type/tone chrome
 */
export * from "./callout";
