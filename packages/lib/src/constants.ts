/**
 * Shared cross-product constant values.
 *
 * @module
 * @categoryDefault Constants
 */

/**
 * Query-parameter name carrying the OAuth flow context, shared by login/consent pages and the API endpoints they post to.
 *
 * @category Constants
 */
export const OAUTH_QUERY_PARAM = "oauth_query";
